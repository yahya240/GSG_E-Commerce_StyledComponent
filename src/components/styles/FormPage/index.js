import styled from "styled-components"

const FormPage = styled.div`
    max-width: 100vw;
    height: 100vh;
    background-color: ${props => props.theme.pallet.bgPrimary};

    display: flex;
    justify-content: center;
    align-items: center;

`

export default FormPage